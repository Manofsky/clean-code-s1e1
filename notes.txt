Buy tickets

git checkout master
git checkout clean-code-s1e1
git checkout notes
git checkout -b clean-code-s1e1
git checkout -b notes

git push origin clean-code-s1e1
git push origin clean-code-s1e1 --force
git push origin notes

git commit -m "docs: update notes"

git rebase -i HEAD~3

git add index.html
git add style.css
git add app.js


git commit -am "refactor: adjust CSS-HTML-JS-formatting according to rule 3.4 in html-and-css.md"

git commit -am "fix: undo formatting fix according to rule 1.2 in html-and-css.md"

git commit -am "fix: fix CSS-HTML-formatting according to rules 1.2 & 3.5 in html-and-css.md"

git commit -am "refactor: adjust CSS-HTML-JS-formatting according to rule 3.3 in html-and-css.md"

git commit -am "refactor: adjust CSS-HTML-JS-formatting according to rule 3.2 in html-and-css.md"

git commit -am "refactor: adjust CSS-HTML-JS-formatting according to rule 3.1 in html-and-css.md"

git commit -am "refactor: adjust CSS-formatting according to rule 3.8 in html-and-css.md"

git commit -am "refactor: adjust CSS-formatting according to rule 3.7 in html-and-css.md"

git commit -am "refactor: adjust CSS-formatting according to rule 3.6 in html-and-css.md"

git commit -am "refactor: adjust CSS-formatting according to rule 3.5 in html-and-css.md"

git commit -am "fix: add alt attributes according to rule 1.2 in html-and-css-extended.md"

git commit -am "refactor: adjust HTML-formatting according to rule 2.5 in html-and-css.md"

git commit -am "fix: remove the type attribute for connecting styles and script according to rule 2.4 in html-and-css.md"

git commit -am "refactor: adjust HTML-formatting according to rule 2.3 in html-and-css.md"

git commit -am "fix: add Html5 DOCTYPE tag according to rule 2.2 in html-and-css.md"

git commit -am "refactor: adjust HTML-and-CSS-formatting according to rule 1.3 in html-and-css.md"

git commit -am "refactor: adjust HTML-and-CSS-formatting according to rule 1.2 in html-and-css.md"

git commit -am "refactor: adjust CSS-formatting according to rule 1.1 in html-and-css.md"

"refactor: indent each nested element according to rules 2.1 & 1.1 in html-and-css.md"

git commit --amend

git commit -am "refactor: indent each nested element according to rules 2.1 in html-and-css.md"

"refactor: select new lines according to rules 2.1 in html-and-css.md"

git commit --amend

git commit -am "refactor: adjust HTML-formatting according to rules 2.1 in html-and-css.md"


В поиске VSCode:
editor.renderWhitespace (настройка отображения пробелов)
сделал all вместо selection

"editor.renderWhitespace": "all",
"editor.tabSize": 2

Замена табуляции отступами
Detect Indentation
Editor: Insert Spaces

расширение prettier для VSCode

Editor: Format On Save


git diff
git show b17c35e

alexom98 - 21 балл

{
  "editor.formatOnSave": false,
  "editor.renderWhitespace": "all",
  "editor.tabSize": 2
}